# DreamLand — Conformalized DreamerV3 + MPPI for Planetary Landing
# Python 3.11 recommended
#
# Install order matters for PyTorch:
#   1. conda activate dreamland
#   2. pip install torch torchvision --index-url https://download.pytorch.org/whl/cu121
#   3. pip install -r requirements.txt
#
# For CPU-only: replace cu121 with cpu in the torch install URL

# ── Core ML ───────────────────────────────────────────────────────────────────
torch>=2.2.0                   # DreamerV3, DeepLabV3+, MC Dropout
torchvision>=0.17.0            # DeepLabV3_ResNet101_Weights, transforms

# ── Numerical / Scientific ────────────────────────────────────────────────────
numpy>=1.26.0
scipy>=1.12.0                  # ndimage.zoom for cost-map downsampling

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib>=3.8.0
pillow>=10.2.0                 # Image loading (PIL)

# ── Path Planning ─────────────────────────────────────────────────────────────
networkx>=3.2.0                # A* graph search

# ── Uncertainty Quantification ────────────────────────────────────────────────
lightning>=2.2.0               # required by lightning-uq-box
lightning-uq-box>=0.1.0        # MC Dropout segmentation + conformal calibration

# ── Vision Foundation Models (optional — Track 2 V-JEPA 2) ───────────────────
transformers>=4.40.0           # HuggingFace: facebook/vjepa2-vitl-fpc64-256
gymnasium>=0.29.0              # RL environment interface (pdg_vjepa2.py)

# ── Utilities ─────────────────────────────────────────────────────────────────
requests>=2.31.0               # NASA API HTTP calls (optional, urllib used by default)
tqdm>=4.66.0                   # progress bars for training loops
